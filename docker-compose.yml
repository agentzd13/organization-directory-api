version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - STATIC_API_KEY=test-secret
      - DATABASE_URL=sqlite+aiosqlite:///./data/app.db
    volumes:
      - ./data:/app/data
